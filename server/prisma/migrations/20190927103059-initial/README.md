# Migration `20190927103059-initial`

This migration has been generated by Zac Tolley at 9/27/2019, 10:30:59 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."User.email"

DROP TABLE "public"."Post";

DROP TABLE "public"."User";

CREATE TABLE "public"."Project" (
  "id" text NOT NULL  ,
  "title" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Todo" (
  "complete" boolean NOT NULL DEFAULT false ,
  "id" text NOT NULL  ,
  "title" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Todo" ADD COLUMN "project" text   REFERENCES "public"."Project"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190927103059-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,22 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = env("POSTGRES_URL")
+}
+
+model Project {
+  id      String @default(cuid()) @id
+  title   String
+  project Todo[]
+
+}
+
+model Todo {
+  id       String   @default(cuid()) @id
+  title    String
+  complete Boolean
+  project  Project?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190927103059-initial)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190927103059-initial'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
